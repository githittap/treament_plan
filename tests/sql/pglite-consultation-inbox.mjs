import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import {pathToFileURL} from 'node:url';

const root=process.cwd(),pkg=process.env.PGLITE_PACKAGE_ROOT;
if(!pkg)throw new Error('PGLITE_PACKAGE_ROOT required');
const {PGlite}=await import(pathToFileURL(path.join(pkg,'dist/index.js')).href),db=new PGlite(),q=s=>db.query(s).then(x=>x.rows);
const manager='22222222-2222-2222-2222-222222222222',staff='33333333-3333-3333-3333-333333333333',blocked='44444444-4444-4444-4444-444444444444';
const setUser=async id=>db.exec(`set role authenticated;select set_config('app.test_uid','${id}',false);`);
try{
  await db.exec(`create role anon;create role authenticated;create role service_role;create schema auth;create or replace function auth.uid() returns uuid language sql stable as $$select nullif(current_setting('app.test_uid',true),'')::uuid$$;grant usage on schema auth to authenticated;grant execute on function auth.uid() to authenticated;create table auth.users(id uuid primary key);create table public.profiles(user_id uuid primary key,role text,active boolean default true,approved boolean default true,account_access_status text default '활성');create or replace function public.my_role() returns text language sql stable security definer as $$select coalesce((select role from public.profiles where user_id=auth.uid()),'')$$;create or replace function public.employee_hub_access_allowed() returns boolean language sql stable security definer as $$select exists(select 1 from public.profiles where user_id=auth.uid() and active and approved and account_access_status='활성')$$;create table public.consultation_journals(id uuid primary key default gen_random_uuid(),patient_name text not null,contact_phone text,source_sheet text not null,consulted_on date not null,status text not null,consultation_note text not null,next_action text,author_id uuid not null default auth.uid(),created_at timestamptz default now(),updated_at timestamptz default now());`);
  const migration=fs.readFileSync(path.join(root,'db/consultation_inbox.sql'),'utf8');
  await db.exec(migration);await db.exec(migration);
  assert.equal((await q("select count(*)::int n from pg_policies where tablename='consultation_inbox'"))[0].n,3);
  const acl=(await q("select has_table_privilege('anon','public.consultation_inbox','select') anon_select,has_table_privilege('authenticated','public.consultation_inbox','delete') auth_delete,has_column_privilege('authenticated','public.consultation_inbox','status','update') status_update,has_column_privilege('authenticated','public.consultation_inbox','journal_id','update') journal_update,has_function_privilege('anon','public.consultation_inbox_convert_to_journal(uuid)','execute') anon_rpc,has_function_privilege('authenticated','public.consultation_inbox_convert_to_journal(uuid)','execute') manager_rpc,has_function_privilege('anon','public.set_consultation_inbox_updated_at()','execute') anon_trigger"))[0];
  assert.equal(acl.anon_select,false);assert.equal(acl.auth_delete,false);assert.equal(acl.status_update,true);assert.equal(acl.journal_update,false);assert.equal(acl.anon_rpc,false);assert.equal(acl.manager_rpc,true);assert.equal(acl.anon_trigger,false);
  await db.exec("set role service_role;select set_config('app.test_uid','11111111-1111-1111-1111-111111111111',false);select set_config('request.jwt.claim.role','service_role',false);");
  const first=await q("select * from public.consultation_inbox_ingest_service('kakao','evt-1','2026-09-22T00:00:00Z','홍길동','01012345678','문의','내용')");
  const again=await q("select * from public.consultation_inbox_ingest_service('kakao','evt-1','2026-09-22T00:00:00Z','홍길동','01012345678','문의','내용')");
  assert.equal(first[0].id,again[0].id);
  await db.exec('reset role;');
  await q(`insert into auth.users values ('${staff}'),('${blocked}'),('${manager}')`);
  await q(`insert into public.profiles values ('${staff}','staff',true,true,'활성'),('${blocked}','manager',true,true,'차단'),('${manager}','manager',true,true,'활성')`);
  await setUser(staff);assert.equal((await q('select * from public.consultation_inbox')).length,0);await assert.rejects(q(`select * from public.consultation_inbox_convert_to_journal('${first[0].id}'::uuid)`),/manager or owner access required/);await db.exec('rollback');
  await setUser(blocked);assert.equal((await q('select * from public.consultation_inbox')).length,0);await assert.rejects(q(`select * from public.consultation_inbox_convert_to_journal('${first[0].id}'::uuid)`),/manager or owner access required/);await db.exec('rollback');
  await setUser(manager);
  await assert.rejects(q(`update public.consultation_inbox set assigned_to='${staff}' where id='${first[0].id}'`),/row-level security/);await db.exec('rollback');
  await setUser(manager);
  await q(`update public.consultation_inbox set status='in_progress',assigned_to='${manager}' where id='${first[0].id}'`);
  const updated=(await q(`select status,assigned_to from public.consultation_inbox where id='${first[0].id}'`))[0];assert.equal(updated.status,'in_progress');assert.equal(updated.assigned_to,manager);
  await assert.rejects(q(`update public.consultation_inbox set message='변조' where id='${first[0].id}'`),/permission denied/);await db.exec('rollback');
  await setUser(manager);
  const converted=await q(`select * from public.consultation_inbox_convert_to_journal('${first[0].id}'::uuid)`);assert.ok(converted[0].journal_id);
  assert.equal((await q(`select status from public.consultation_inbox where id='${first[0].id}'`))[0].status,'converted');
  await db.exec('reset role;');assert.equal((await q('select count(*)::int count from public.consultation_journals'))[0].count,1);await setUser(manager);
  await assert.rejects(q(`update public.consultation_inbox set status='new' where id='${first[0].id}'`),/row-level security/);await db.exec('rollback');await setUser(manager);
  await assert.rejects(q(`update public.consultation_inbox set journal_id=null where id='${first[0].id}'`),/permission denied/);await db.exec('rollback');await setUser(manager);
  await assert.rejects(q(`select * from public.consultation_inbox_convert_to_journal('${first[0].id}'::uuid)`),/already converted/);await db.exec('rollback');
  const rollback=fs.readFileSync(path.join(root,'db/consultation_inbox_rollback.sql'),'utf8');
  await db.exec('reset role;');await assert.rejects(db.exec(rollback),/contains data/);await db.exec('rollback');
  await db.exec('delete from public.consultation_inbox;');await db.exec(rollback);
  assert.equal((await q("select to_regclass('public.consultation_inbox') is null gone"))[0].gone,true);
  assert.equal((await q("select count(*)::int count from pg_proc where pronamespace='public'::regnamespace and proname in('set_consultation_inbox_updated_at','consultation_inbox_assignee_allowed','consultation_inbox_convert_to_journal','consultation_inbox_ingest_service')"))[0].count,0);
  console.log('PGLITE_CONSULTATION_INBOX_PASS');
}finally{await db.close();}
