# todos — 내부 도구(T8/D) 작업 목록

갱신 2026-09-06 · 상세 상황판: `Z:\09_claude-output\04_AI·Claude운영\산출물\치과내부도구_상황판.html`

## 🧭 이번 세션 인수인계 (2026-09-07~08 · 대량 기능 배포, 다음=M3 급여) — compact 대비
> 이 세션에서 8건 배포 완료(전부 push됨): 입금P1검증·교정v1.2·재스캔·접근제어·탭권한(역할+사람별)·PWA·연차개선·비밀기록원장전용 + 결과보고서. 아래는 상세.
- **목적**: 지난 세션이 남긴 "다음 실입금 때 자동 검증" 체크포인트 확인 — Claude가 Supabase를 직접 조회해 입금 피드 P1이 실제로 도는지 검증.
- **확인 결과**: `deposits` 표 총 171건(07-31~09-04). 08-15~08-22 공백(9일 침묵, 기존에 알려진 것) 이후 08-23 당일 실입금 1건이 바로 잡혔고, 이후 영업일(평일)은 하루도 안 빠지고 매일 6~10건씩 정상 적재(주말 공백은 휴진이라 정상). **P1 핵심 파이프라인(SMS→웹훅→DB→hr 입금탭) 실가동 검증 완료.**
- **원장 확인 3건 모두 답변 완료(09-06)**: ①텔레그램 `TELEGRAM_CHAT_ID` **직원 그룹으로 교체 완료** — P1 텔레그램 알림까지 포함해 전부 완결. ②로컬 미커밋 변경 3건 **commit 승인**받아 커밋 완료(아래). ③`deno.lock` **커밋 방식**(하드코어=재현 가능한 빌드+의존성 무결성 고정, `package-lock.json`과 동일 원칙)으로 결정 — 추적 시작.
- **커밋 내역**: `fix(deposit-webhook)` 텔레그램 실패 로깅(이미 배포된 v12와 동기화) · `chore` deno.lock 추적 시작 · `docs` 운영 규약(.claude-project.json 연동) + todos.md 갱신. **push는 아직 안 함**(공개 repo라 별도 승인 대기).
- **교정보드 v1.2 완료·배포**: codex exec로 4개 기능(칸반↔표 토글·CSV·FDI 픽커·문구버튼) 구현, Claude가 JS 구문·CSV 이스케이프·FDI 해부학적 배치·SQL 멱등성 검수 완료. `app_settings.ortho_quick_phrases` 시드 SQL 원장 승인 후 실행 완료. 코드 커밋 `2771c09`. **원장 할 일**: 운영 계정으로 표 전환·CSV·FDI·문구칩 실사용 확인만 남음(급하지 않음).
- **세션 중 새로 들어온 요청 4건**:
  1. **로그인 승인제** + 2·3. **비밀 진료기록** → **구현·배포 완료(2026-09-07, 커밋 `67d1387`)**. 스펙 `docs/superpowers/specs/2026-09-06-access-control-design.md`, 구현계획 `docs/superpowers/plans/2026-09-07-access-control-plan.md`. `profiles.approved`(기존 14명 전원 승인 백필)+`my_role()` pending 분기 + **ortho/기공 6개 데이터표 RLS를 `my_role()<>'pending'`으로 조임**(설계 전제였던 "using(true) 자동차단"이 거짓임을 실측으로 발견해 보강 — 미승인자가 교정보드·기공차트 직접 접근하던 구멍 차단). hr.html: 승인대기 게이트 + 원장탭 승인목록 + "비밀 진료기록" 탭(명단 밖이면 미렌더) + 접근명단 관리. `confidential_access`(고정명단)+`confidential_records`(환자명·차트번호 포함). SQL 실행·검증(pending 0·게이팅 완료) 완료. **원장 할 일**: 신규 테스트계정 가입→승인 플로우 + 비밀기록 탭 실사용 확인.
  4. **[교정, 별도 건] 재스캔(리스캔) 란 — 완료**(2026-09-07): 리메이크 플래그 완전 제거 → 재스캔으로 대체. `ortho_rescans` 표(RLS를 `ortho_visits`와 DB에서 직접 대조해 정책 문구까지 일치 확인) + 케이스 상세 "재스캔" 탭(계획제출일+3년 무상마감 카운트다운, 재스캔일 바꾸면 무상/유상 자동재계산, 당일만 수정) + 카드 배지·필터칩 + "재스캔 관리" 서랍(무상마감 90일 이내·종료 강조) + 실시간구독. 계획제출일은 `flags.plan_submitted_at`(스키마 변경 없음). SQL 실행 완료(표·realtime 등록 확인)·커밋 `4d37091`. **교정진단기(`ortho_integrated_chart`) 연동은 불가로 결론** — 로컬 JSON 저장/불러오기만 있고 공유 DB가 없어 상시 연동 접점 없음. 수동입력으로 확정, 연동은 백로그(B: JSON 다리 / C: Supabase 이전)로 이동.
- **직원별 탭 노출 권한 완료·배포**(2026-09-07): (a)역할별 — 원장 탭 "🧭 탭 노출 설정" 표(`hr_tab_roles`) (b)사람별 예외 — "👤 사람별 탭 예외"(직원 선택→탭별 기본/강제보이기/강제숨기기, `hr_tab_overrides`). 우선순위: 개인예외>역할규칙, owner는 항상 전부. 둘 다 app_settings 저장(코드 재배포 불필요)·**메뉴 숨김만**(RLS 안 건드림, 원장 결정). 커밋 `df989b9`·`7f1a4bb`.
- **PWA(홈화면 설치형 앱) 완료·배포**(2026-09-07, 커밋 `3f1ae90`): hr·ortho·index에 매니페스트 3종+서비스워커(`sw.js`, 캐시없이 네트워크통과)+아이콘5종(`icons/`, PIL로 logo.png에서 생성)+apple 메타. 폰에서 "홈 화면에 추가"로 앱처럼 전체화면 실행. 기존 로직 불변(head+SW등록만).
- **연차 개선 + 비밀기록 원장전용 완료·배포**(2026-09-08, 커밋 `4ae0704`): 연차=신청서 양식(종이양식 재현·📄버튼·인쇄)+결재 CSS스탬프(실물도장X, 위조방지)+동시연차(승인+대기 합산, 그날 다른2명 차단·1명 특별사정)+연락처(`leave_requests.contact`). 비밀기록=`confidential_records.owner_only`+RLS(non-owner는 owner_only행 제외)+원장만 체크박스+🔒배지. ⚠️ **연차 1차결재 실작동엔 chief 지정 필요**(현재 원장 빼고 전원 staff — 계정채우기 때). 신청서 인쇄 @media print 격리는 후속.
- **다음 순번**: **M3 급여·명세서**(급여대장 가져오기+임금명세서 발행+휴일근로 계산+월말평가+조퇴공제). 백엔드 표(payroll_rows·payslips·monthly_reviews·bonus_rules)는 이미 있음(owner RLS 확인함), 화면만 추가. 그 후 계정채우기(chief/manager 지정+25명 가입 유도).
- **M3 설계 완료(2026-09-08)**: 원장 확인 4건(급여대장=엑셀+수기 둘다·명세서=법정양식+병원컬러·시급=profiles필드추가·월말평가=자유메모형) 받고 설계서 작성. 조사 중 갭 2건 발견해 같이 반영: ①지문엑셀 파서가 조퇴(early)분을 이미 계산해 화면엔 보여주면서 저장은 안 하고 있었음 → `early_min` 컬럼만 추가하면 끝 ②`profiles`가 `using(true)`라 시급을 거기 넣으면 전 직원이 서로 볼 수 있어 `wage_info` 별도 owner전용 표로 조정 제안(원장 재확인 1건, 위 항목 참조). 설계서: `docs/superpowers/specs/2026-09-08-payroll-payslip-design.md`. **다음 행동**: 원장이 wage_info 제안 확인 + "코덱스 위임해" 지시하면 codex 착수.

👉 **새 세션 지시문**: `overview.md·todos.md 읽고 이어서 해줘. "🔵 진행 중 / 바로 다음" 1번부터. 컨텍스트 80% 넘으면 멈추고 todos.md에 적고 알려줘.`

## 아직 원장이 답하지 않은 것
- **계정 관리 기능 2가지 추가할지 결정 필요** (2026-09-09 질문 답변) — ①**비밀번호 찾기 없음**: 지금은 로그인/가입만 있고 재설정 기능이 없어, 비번 분실 시 원장이 Supabase 대시보드(supabase.com/dashboard/project/texevhsxttfoqkrucfzl)에서 직접 처리해야 함. 앱에 "비밀번호 재설정 이메일" 기능을 추가할지. ②**탈퇴(계정 삭제)**: 완전 삭제는 위험(FK 확인함 — attendance·wage_info만 같이 지워지고 연차·급여대장·근무표 등은 주인 없는 데이터로 남아 되돌릴 수 없음) → 대신 이미 있는 `profiles.approved`를 취소하면 로그인은 되지만 데이터 접근이 전부 막히는 안전한 "계정 정지"가 가능한데, 지금 원장 탭에 승인 취소 버튼이 없음. 추가할지.
- 🔴 **연차 4건 대기중(2026-09-09 발견) — 원장 답변 일부만 처리됨**: 실장(임은숙·테스트)이 5주 넘게 아무것도 처리 안 해 원장 화면에도 안 보이고 있었음(원장 화면이 `1차승인`만 보여주는 구조였음, 이번에 원장 직접승인 경로 추가 중). 원장 답변: 신동광(9/9)=보류, 정용태본인(9/8)=테스트였음(조치 불필요), 김수란(8/25)·이소연(8/19)=보류. **4건 모두 DB 그대로 손 안 댐** — 화면 고쳐지면 원장이 직접 처리. ⚠️ 임은숙 실장이 실제로 로그인해서 쓰고 있는지 확인 필요(5주간 무반응).

👉 **읽는 곳(HTML)**: `docs\superpowers\_보기\specs\2026-09-08-payroll-payslip-design-rev1.html`

- ⏳ **노무법인 질문지 v2(18문항) 발송** — `Z:\09_claude-output\02_노무·법률·행정\급여대장_산출기준_확인요청_2026-09-08.html`. **2026-09-09 개정**: 법령 검색으로 32문항 중 상당수가 이미 해소돼 18문항으로 축소함(아래 참조). 서광(문서윤 노무사)께 보내고 답을 받아야 나머지가 확정된다.
- ✅ **4대보험 요율 법령 검증 완료 (2026-09-09)** — 국민연금공단·보건복지부 공식 발표 검색 대조 결과, 설계서에 이미 뽑아둔 계산식(국민연금 9.5%·건강보험 7.19%·장기요양=건강보험료×13.14%) **전부 정확히 일치**. 요율 자체는 더 이상 노무법인에 안 물어봐도 됨.
- 🔴 **세액표 문제 — 추측 아니라 확정 사실** — 국세청이 2026-02-27 근로소득 간이세액표를 개정해 **2026-03-01 지급분부터 적용** 공표함(검색 확인). 급여대장 파일의 세액표 시트명은 `2023세액표`. **2026년 급여에 3년 전 세액표를 쓰고 있는 것이 사실로 확정됨.** 세액표 갱신 요청을 질문지 1번으로 넣음(연말정산에서 정산되므로 최종 세부담은 같고 매달 실수령액 타이밍만 달라짐 — 급한 문제는 아님).
- 참고: 국민연금 세부내역은 국민연금공단 EDI(4insure.or.kr)에서 원장님이 직접 조회 가능, 고용보험료율은 근로복지공단에서 확인 가능 — 노무법인 아니어도 됨.
- ⏳ **선행조건 4건(데이터 입력, 원장/실장이 해야 함)** — 기능이 아니라 데이터라 Claude가 못 채운다:
  1. **지문엑셀을 실제로 한 번 업로드** (`attendance` 0행 — 회신서식 자동생성·조퇴공제·월집계가 전부 빈 화면)
  2. **`profiles.hire_date` 15명 채우기** (전원 비어 있어 연차 자동산정 불가)
  3. **근무표(`schedules`) 채우기** (2주치뿐 — 연차 일수 계산이 fallback으로만 돔)
  4. `wage_info`에 시급·월소정근로시간(209/167/157)·고정상여·숙소지원 입력
- ⏳ **워드로 열어둔 `.md` 닫기** — 초판 설계서가 잠겨 있어 rev1을 별도 파일로 냈다. 닫으면 합친다.

### ✅ 2026-09-09 근태.zip(메모+스크린샷6장) UI 개선 요청 8건 — 전부 배포 완료
> 근태 지문데이터 아니라 hr.html·ortho.html 실사용 피드백. 원장 답변(Q1=자동반영, Q2=드래그채우기) 반영해 codex 위임 구현. Claude가 diff 직접검수+node구문검사 후 배포. 커밋 `2c6cadf`(hr.html) · `73cde93`(ortho.html).
1. **연차신청서** — 실제 종이양식과 대조 확인, 이미 일치해 재작업 안 함.
2. **소속(부서) 입력** — 원장 탭에 인라인 편집(진료실/데스크/기공팀/기타) 추가. `profiles.dept`가 15명 전원 비어있던 게 이제 채울 수 있게 됨(아직 안 채워짐 — 원장 할 일).
3. **연차→근무표 자동표시** — 승인된 연차를 근무표에 배지로 자동 표시(schedules에 안 씀, 순수 표시). 연차 신청 모달에 겹치는 동료 이름도 표시.
4. **근무표**: 드래그로 여러 칸 한 번에 채우기 + 근무/off/야간(빨강)/기타로 교체(half_am·half_pm 제거, DB 제약조건 적용 완료) + 진료실→데스크→기공팀→미지정 그룹핑.
5. **재스캔**: 기본값=무상, 유상 선택 시 사유 필수, 미래 날짜="예정" 배지+재스캔 관리 서랍에 예정 섹션.
6. **교정 상세**: 주민번호·유형·트림·차트번호 접이식(기본 숨김).
- ⏸️ **월별 캘린더(샤프트 먼저 선택→직원 배정)** — 별도로 미룸(이번 배치 안 함, 원장이 요청하시면 별도 설계).
- ⚠️ **구현 중 발견한 사실(고침 아님, 기존 설계)**: 일반 직원은 다른 직원의 **승인된** 연차 이름만 보임 — 대기·1차승인은 RLS상 실장·원장만 조회 가능. 예전부터 있던 제약(이번에 새로 생긴 제한 아님), 원장이 원하시면 별도로 RLS 설계 필요.
- 📌 **사소한 미정리**: `db/hr_schema.sql`(from-scratch 부트스트랩용)에 아직 옛 half_am/half_pm 제약이 남아있음 — 실제 운영 DB엔 영향 없음(schedules_shift.sql로 이미 교체됨), DB를 처음부터 새로 만들 때만 문제. 급하지 않아 백로그로.

### (완료) 2026-09-08 Q1~Q5 원장 답변 전부 받음
- **Q1 연차 일수** → 특정 요일 빼지 않음. **근무표 기준**(그 사람 근무일인데 추가로 쉬면 연차). 원장: "우리는 스케줄 근무, 직원마다 스케줄이 다르다"
- **Q2 결재** → **둘 다.** chief = 임은숙·테스트 **지정 완료(DB 확인함)** + 공석 대비 fallback 추가
- **Q3 시급 저장** → `wage_info` 별도 owner전용 표 **승인**
- **Q4 세액표** → 노무법인 질문지 8번에 포함
- **Q5 회신서식 자동생성** → **채택.** + 고정 상여금(구두 인상분)·숙소지원비(임대인 직접 송금)도 함께
- **작업 방식** → 워크플로는 매번 묻지 말고 **80% 이상 효과적일 때만** Claude가 판단해 돌린다(전역 메모리 기록)

### ⚠️ 앞선 보고 2건 정정 (Supabase 직접 조회로 확인)
1. **"연차·조퇴에서 이미 손해가 났을 수 있다" → 아직 손해 없음.** `attendance` 0행, `leave_ledger` 0행, `leave_requests` 2건 모두 1일짜리. **지금 고치면 손실 0.**
2. **"현재 노무법인 = 제이앤" → 틀림.** 2025년이 제이앤, **2026년 3월부터 서광(문서윤 노무사)**. 파일에 남은 제이앤 흔적은 옛 템플릿을 이어받은 것.

### (완료) 2026-09-08 원장 신규 요구 5건 처리
①예스폼 서식 참고 → 5개 파일 분석 완료, 설계서 F·G장 반영 ②보고 md 금지 → `_보기\` 켜서 HTML 자동생성 ③문서 서식 원장 편집 → `doc_templates` 표 설계(E장) ④4대보험·소득세 자동계산 → **부분가능**, 3단계 자립 로드맵으로 설계(0장·C장) ⑤상여금 → 규칙 엔진 안 만들고 자유입력 유지

### 이번 세션 연차·비밀기록 개선 — 완료·배포(2026-09-07, 커밋 `4ae0704`)
- **A. 연차**: ①신청서 양식(원장 종이양식 재현: 결재란·성명·소속·휴가종류·기간·사유·연락처·기타·신청인) — 각 신청에 "📄 신청서" 버튼, 인쇄 가능. ②결재 스탬프 = **CSS "승인·이름·날짜"**(실물도장 아님 — profiles 전원 열람가능이라 실물 도장 넣으면 위조위험, 그래서 스탬프마크로. 대외 실물도장은 `stamp_html.py` 유지). ③**동시연차: 승인+대기 합산, 해당일 다른 2명이면 신청차단·1명이면 특별사정 사유필수**(날짜별 정확 카운트). ④연락처칸+`leave_requests.contact`.
- **B. 비밀기록**: `confidential_records.owner_only` + RLS(non-owner는 owner_only 행 제외). 원장만 "원장 전용" 체크박스, 목록 🔒 배지. (교정차트 필드이식은 원장이 "간단유지" 택해 안 함.)
- ⚠️ **운영상 필요**: 현재 **chief(실장)·manager 지정된 사람이 아무도 없음**(원장 빼고 전원 staff). 연차의 "중간관리자 1차 결재"가 실제로 돌려면 원장 탭에서 누군가를 chief로 지정해야 함 → 계정채우기 때 함께.
- ⚠️ **미세 폴리시(후속)**: 신청서 인쇄가 window.print라 인쇄 시 뒤 화면도 같이 나올 수 있음(@media print 격리 안 함). 필요시 후속.

> ⚠️ **codex 실행 함정(2026-09-07 발견·중요)**: `codex -a never exec -s workspace-write`가 이 프로젝트 경로(한글·공백·괄호)에서 "복수 쓰기 경로 설정 오류"로 apply_patch를 거부하는 경우가 있다(연속 실패 관측). **`codex exec --dangerously-bypass-approvals-and-sandbox -C <projDir>`로 우회하면 성공**(프롬프트로 대상 파일 제한 + Claude가 git diff로 blast radius 점검하면 안전). 오늘 탭설정·사람별예외·PWA 전부 이 방법으로 배포됨. **다음 세션은 처음부터 우회모드 권장.**

## 🎯 한 줄 비전 (2026-07-30 원장 확정)
설치·외주 없이 브라우저로 굴리는 아산정플란트 내부 운영 세트 — 진료·기공·교정·인사 4축, 로그인·데이터는 Supabase 한 곳.

## 🔵 진행 중 / 바로 다음
> 우선순위: 입금피드 → 교정 v1.2 → 급여 → 계정 (앞 2개 완료, 이제 급여)
- [x] ~~**1) M3 급여 1단계** — 급여대장·시급설정 탭 배포 완료(2026-09-08, 커밋 `85bb0bf`)~~ 아래 「최근 완료」 참조.
- [ ] **1-2) M3 급여 2단계** ⬅️ **바로 다음**: 명세서 발행(payslips, 법정 9항목+병원컬러) · 4대보험·소득세 검산 계산(건강 3.595%·장기요양=건강보험료×13.14%·고용 0.9%·지방세=소득세10%, 요율은 app_settings 기간세트, 간이세액표 income_tax_table 표 이식) · doc_templates 서식 편집기(치환엔진+법정9항목검증) · 노무법인 회신서식 자동생성(근태집계) · 연차 일수 근무표기준 정정(A-1) · 연차 결재 안전화(A-2 RPC). 설계서 rev1 참조.
- [ ] **2) 계정 채우기**: chief(실장)·manager 지정(연차 1차결재 위해 필요) + 직원 25명 셀프 가입 유도 + 미가입 독려·권한 일괄지정 화면
- [x] ~~교정보드 v1.2~~ 완료(2026-09-07)

## ⏸️ 대기 (원장 결정·자료 필요)
- [ ] **UI 브랜드 리스킨** — 레퍼런스(취향·무드·벤치마크) 모이면 **한 번에** frontend-design 스킬로. 브랜드 KB=`Z:\09_claude-output\06_KB\아산정플란트_이미지자산_KB`(로고·#156f72·실사)
- [ ] 온보딩 미제출 **페널티** 구현: 제출 마감일 표시 + 미제출자 관리자 **SMS 보고** + 근무표 열람 제한
- [ ] 대체공휴일 2건(8/17·10/5) 실제 적용 여부 — 병원은 휴일 원칙근무·단축 많음, 월계산 시 **실근무시간만** 보고
- [ ] 서식 레퍼런스(계약서 등) + 병원 컬러 입히기 → M2 계약서 템플릿 때

## 📋 백로그 (설계 완료·구현 대기)
- [ ] **D2 M2 잔여**: 계약서 자동생성+도장+전부서명+5일 열람 / 통합 캘린더(이벤트·off)
- [ ] **입금 피드 P2/P3**: (P2) 입금↔교정 진단비 자동 매칭 · (P3) 가상계좌 · 텔레그램 알림 문구 정제(입금만·금액/입금자 포맷)
- [ ] **D5 작업판 자동기록**: CC 세션 종료 훅 + 협업 CLAUDE.md 관례 → write RPC(다단계 작업만). SQL은 원장 실행
- [ ] **D6 기공차트 탭 전환 + 이미지 첨부**: 장부 세로나열→탭 / 행에 사진 업로드·클립보드 붙여넣기 영구저장(→리메이크 전략)
- [ ] **T6 리뷰봇·고객DB**: `03_병원운영·전산\내부홈페이지\T6_리뷰봇·고객DB_연동사양.md` — profiles.role 재사용, cs_events/reviews_log
- [ ] 내부 평가 시스템: 부서(진료실·데스크·홍보팀·상담팀·기공팀)별, 항목 러프(기공 리메이크 등) — 추후 구체화
- [ ] 스크래핑(Playwright): SureSmile 배송·Tracking 자동수집 / 덴트웹 교정상태 (2FA 없음 확인) — 교정보드 P3
- [ ] **교정진단기 연동**: `Z:\09_claude-output\05_임상·진료\진료기록부\ortho_integrated_chart_최종본_교정.html`("교정 통합 진단 시스템")은 로컬 JSON 내보내기/불러오기만 있고 공유 DB가 없어 교정보드와 상시 자동연동 불가(2026-09-07 확인). 필요해지면 B(JSON 다리, 반자동) 또는 C(그 도구를 Supabase로 재설계, 큰 작업) 중 선택.

## 🧩 로드맵 밖 (재미도구 — 유지만, 개발순위 제외)
- 사주 · 직원뽑기 · 뉴스 · 보철프로토콜_진단기 · 설명덱_제작기

## ✅ 최근 완료
- **M3 급여 1단계 배포** (2026-09-08, 커밋 `85bb0bf`): `db/payroll.sql`(attendance.early_min + wage_info 이력형 표·owner RLS) Supabase 적용·검증 완료 + hr.html 💰급여 탭(owner전용, 탭권한 override 제외) — 시급설정(wage_info 편집) + 급여대장(노무법인 엑셀 2행헤더 병합·헤더문자열 별칭매칭·긴별칭우선·차인지급액 이후 보조열 제외·미매핑 제외 → payroll_rows 단일 upsert) + 조퇴 저장 누락(A-3) 보정. codex 위임 구현 + Claude 검수, **실제 8월 급여대장으로 파서 직접 검증**(15명·전열 정상매핑·net 일치·중복합산0)·JS구문통과. ⚠️ **원장 할 일**: owner 로그인해 급여탭 실사용(엑셀 업로드→저장, 시급설정)·실제 화면 확인 — Claude는 로그인 못 해 브라우저 최종테스트 미실행. codex의 "hr.html gitignore" 보고는 오판(정상추적 확인).
- **입금 피드 P1 완결** (2026-09-06): Claude가 Supabase `deposits` 표·Edge Function 배포본 직접 조회 — 07/31~09/04 총 171건, 08/23 복구 이후 영업일 무결락 적재 확인. 텔레그램 `TELEGRAM_CHAT_ID` 직원 그룹 교체도 원장 확인 완료. **hr 입금탭 + 텔레그램 알림 모두 실가동, P1 종료.**
- **입금 피드 P1 코드·백엔드 배포** (2026-07-30): deposits 표(멱등)·Edge Function `deposit-webhook`·hr '입금' 탭. codex 위임·Claude 검수·파서 7/7. 배포 `4f7567e`·`1ce6517`.
- **직원 셀프 회원가입(D2) 라이브** (2026-07-30): 로그인/가입 토글·doSignup(이름→user_metadata)·onAuthed 자동 staff + 정책 `profiles_insert_self` + 원장 실행(SQL·가입 켜기·이메일확인 OFF) + 배포 `25f8f22`
- 교정 케이스 보드(ortho.html) 배포·검증 5/5 · jung-plant.com 연결
- 근태 hr.html: M1(로그인·role·지문엑셀 파서·연차·결재·공지·온보딩) + M2(근무표·출퇴근 연동·조퇴·결근확인)
- 백엔드 SQL(Codex 작성·교차검증): ortho·hr(표24)·app_settings

## 원장 결정 대기 (요약)
`AGENTS.md`·`docs/superpowers` 삭제 7건 커밋 여부(현재 복원됨) · 리스킨 레퍼런스 · 온보딩 페널티 세부 · 대체공휴일 확인 · 내부평가 항목
